% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disproportionality_analysis.R
\name{render_forest_table}
\alias{render_forest_table}
\title{Render a Forest Plot Table from Disproportionality Data}
\usage{
render_forest_table(disproportionality_df)
}
\arguments{
\item{disproportionality_df}{A \code{data.table} containing the following columns:
\itemize{
\item \code{nested}: Type of analysis (e.g., "Crude", "Adjusted")
\item \code{D_E}: Observed count
\item \code{expected}: Expected count
\item \code{IC_median}: Median Information Component
\item \code{IC_lower}: Lower bound of the 95\% confidence interval
\item \code{IC_upper}: Upper bound of the 95\% confidence interval
}}
}
\value{
A \code{forestplot} object representing the forest plot table.
}
\description{
This function takes a data frame containing disproportionality analysis results
and generates a forest plot table using the \code{forestplot} package. It highlights
results with lower confidence intervals greater than zero in red.
}
\details{
The function:
\itemize{
\item Formats the input data for display
\item Highlights rows with significant IC values (lower CI > 0) in red
\item Adds horizontal lines and zebra striping for readability
\item Sets custom x-axis ticks and labels
}
}
\examples{
\dontrun{
library(data.table)
df <- data.table(
  nested = c("Crude", "Adjusted"),
  D_E = c(10, 8),
  expected = c(5.5, 6.2),
  IC_median = c(1.2, 0.8),
  IC_lower = c(0.5, -0.2),
  IC_upper = c(1.9, 1.3)
)
render_forest_table(df)
}

}
